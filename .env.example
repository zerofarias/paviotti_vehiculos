# ==============================================
# 🔒 CONFIGURACIÓN DE SEGURIDAD
# ==============================================

# Base de Datos
DATABASE_URL="mysql://user:password@localhost:3306/paviotti_fleet"

# JWT Secrets (¡CAMBIAR EN PRODUCCIÓN!)
JWT_SECRET="paviotti-fleet-secret-CAMBIAR-EN-PRODUCCION-usar-openssl-rand-base64-32"
JWT_REFRESH_SECRET="paviotti-refresh-secret-CAMBIAR-EN-PRODUCCION-usar-openssl-rand-base64-32"
JWT_EXPIRATION="1h"
JWT_REFRESH_EXPIRATION="7d"

# SSO - Single Sign-On (para login desde sistemas externos)
SSO_SECRET="sso-secret-compartido-entre-sistemas-CAMBIAR"

# Servidor
PORT=3001
NODE_ENV="development"

# Frontend URL (para CORS)
FRONTEND_URL="http://localhost:5173"

# ==============================================
# 📧 CONFIGURACIÓN DE NOTIFICACIONES (Opcional)
# ==============================================

# Email para enviar alertas automáticas
EMAIL_ALERTS_ENABLED=true
NOTIFICATION_EMAIL="lautarosistemas@gmail.com"

# Configuración SMTP (para envío de emails)
SMTP_HOST="mail.paviotti.com.ar"
SMTP_PORT="587"
SMTP_USER="soporte@paviotti.com.ar"
SMTP_PASSWORD="Paviotti36132"

# Ejemplos de SMTP comunes:
# Gmail: smtp.gmail.com:587 (requiere "App Password")
# Outlook: smtp.office365.com:587
# SendGrid: smtp.sendgrid.net:587
# Mailgun: smtp.mailgun.org:587

# API Externa para notificaciones (opcional)
EXTERNAL_API_URL=""
EXTERNAL_API_KEY=""
WEBHOOK_SECRET=""

# ==============================================
# 🔑 CÓMO GENERAR SECRETOS SEGUROS
# ==============================================

# En terminal (Linux/Mac):
# openssl rand -base64 32

# En PowerShell (Windows):
# [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))

# Generar online (usar con cuidado):
# https://randomkeygen.com/
